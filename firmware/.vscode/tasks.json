{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "command": "scripts/build.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "doxygen",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "command": "scripts/doxygen.sh",
            "group": "build"
        }
    ]
}
